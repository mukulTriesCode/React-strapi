import type { RFCType } from "../../types"

const Loading: RFCType = () => {
  return (
    <div className="loader">
        <div className="loader-spinner"></div>
    </div>
  )
}

export default Loading